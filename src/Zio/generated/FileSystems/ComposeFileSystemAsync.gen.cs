// <auto-generated/>
// This file was generated by Zio.AsyncCodeGen. Do not edit this file directly.
#nullable enable
#if NET10_0_OR_GREATER && !ZIO_NO_ASYNC
using System;
using System.Collections.Generic;
using System.Diagnostics;
using System.Diagnostics.CodeAnalysis;
using System.IO;
using System.Linq;
using System.Runtime.CompilerServices;
using System.Threading;
using System.Threading.Tasks;
using static Zio.FileSystemExceptionHelper;

namespace Zio.FileSystems;

/// <summary>
/// Async counterpart to <see cref = "ComposeFileSystem"/>.
/// </summary>
public abstract class ComposeFileSystemAsync : FileSystemAsync
{
    protected bool Owned { get; }

    /// <summary>
    /// Initializes a new instance of the <see cref = "ComposeFileSystem"/> class.
    /// </summary>
    /// <param name = "fileSystem">The delegated file system (can be null).</param>
    /// <param name = "owned">True if <paramref name = "fileSystem"/> should be disposed when this instance is disposed.</param>
    protected ComposeFileSystemAsync(IFileSystemAsync? fileSystem, bool owned = true)
    {
        Fallback = fileSystem;
        Owned = owned;
    }

    /// <inheritdoc/>
    protected override async ValueTask DisposeAsync(bool disposing)
    {
        if (disposing && Owned)
        {
            if (Fallback is not null)
            {
                await Fallback.DisposeAsync().ConfigureAwait(false);
            }
        }
    }

    /// <summary>
    /// Gets the next delegated file system (may be null).
    /// </summary>
    public IFileSystemAsync? Fallback { get; }

    /// <summary>
    /// Gets the next delegated file system or throws an error if it is null.
    /// </summary>
    [DebuggerBrowsable(DebuggerBrowsableState.Never)]
    public IFileSystemAsync FallbackSafe
    {
        get
        {
            if (Fallback is null)
            {
                throw new InvalidOperationException("The delegate filesystem for this instance is null");
            }

            return Fallback;
        }
    }

    protected override string DebuggerDisplay()
    {
        return $"{base.DebuggerDisplay()} (Fallback: {(Fallback is FileSystemAsync fs ? fs.DebuggerKindName() : Fallback?.GetType().Name.Replace("FileSystem", "fs").ToLowerInvariant())})";
    }

    /// <inheritdoc/>
    protected override async ValueTask CreateDirectoryAsyncImpl(UPath path, CancellationToken cancellationToken)
    {
        await FallbackSafe.CreateDirectoryAsync(ConvertPathToDelegate(path), cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <inheritdoc/>
    protected override async ValueTask<System.Boolean> DirectoryExistsAsyncImpl(UPath path, CancellationToken cancellationToken)
    {
        return await FallbackSafe.DirectoryExistsAsync(ConvertPathToDelegate(path), cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <inheritdoc/>
    protected override async ValueTask MoveDirectoryAsyncImpl(UPath srcPath, UPath destPath, CancellationToken cancellationToken)
    {
        await FallbackSafe.MoveDirectoryAsync(ConvertPathToDelegate(srcPath), ConvertPathToDelegate(destPath), cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <inheritdoc/>
    protected override async ValueTask DeleteDirectoryAsyncImpl(UPath path, bool isRecursive, CancellationToken cancellationToken)
    {
        await FallbackSafe.DeleteDirectoryAsync(ConvertPathToDelegate(path), isRecursive, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <inheritdoc/>
    protected override async ValueTask CopyFileAsyncImpl(UPath srcPath, UPath destPath, bool overwrite, CancellationToken cancellationToken)
    {
        await FallbackSafe.CopyFileAsync(ConvertPathToDelegate(srcPath), ConvertPathToDelegate(destPath), overwrite, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <inheritdoc/>
    protected override async ValueTask ReplaceFileAsyncImpl(UPath srcPath, UPath destPath, UPath destBackupPath, bool ignoreMetadataErrors, CancellationToken cancellationToken)
    {
        await FallbackSafe.ReplaceFileAsync(ConvertPathToDelegate(srcPath), ConvertPathToDelegate(destPath), destBackupPath.IsNull ? destBackupPath : ConvertPathToDelegate(destBackupPath), ignoreMetadataErrors, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <inheritdoc/>
    protected override async ValueTask<System.Int64> GetFileLengthAsyncImpl(UPath path, CancellationToken cancellationToken)
    {
        return await FallbackSafe.GetFileLengthAsync(ConvertPathToDelegate(path), cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <inheritdoc/>
    protected override async ValueTask<System.Boolean> FileExistsAsyncImpl(UPath path, CancellationToken cancellationToken)
    {
        return await FallbackSafe.FileExistsAsync(ConvertPathToDelegate(path), cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <inheritdoc/>
    protected override async ValueTask MoveFileAsyncImpl(UPath srcPath, UPath destPath, CancellationToken cancellationToken)
    {
        await FallbackSafe.MoveFileAsync(ConvertPathToDelegate(srcPath), ConvertPathToDelegate(destPath), cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <inheritdoc/>
    protected override async ValueTask DeleteFileAsyncImpl(UPath path, CancellationToken cancellationToken)
    {
        await FallbackSafe.DeleteFileAsync(ConvertPathToDelegate(path), cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <inheritdoc/>
    protected override async ValueTask<System.IO.Stream> OpenFileAsyncImpl(UPath path, FileMode mode, FileAccess access, FileShare share, CancellationToken cancellationToken)
    {
        return await FallbackSafe.OpenFileAsync(ConvertPathToDelegate(path), mode, access, share, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <inheritdoc/>
    protected override async ValueTask<System.IO.FileAttributes> GetAttributesAsyncImpl(UPath path, CancellationToken cancellationToken)
    {
        return await FallbackSafe.GetAttributesAsync(ConvertPathToDelegate(path), cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <inheritdoc/>
    protected override async ValueTask SetAttributesAsyncImpl(UPath path, FileAttributes attributes, CancellationToken cancellationToken)
    {
        await FallbackSafe.SetAttributesAsync(ConvertPathToDelegate(path), attributes, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <inheritdoc/>
    protected override async ValueTask<System.DateTime> GetCreationTimeAsyncImpl(UPath path, CancellationToken cancellationToken)
    {
        return await FallbackSafe.GetCreationTimeAsync(ConvertPathToDelegate(path), cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <inheritdoc/>
    protected override async ValueTask SetCreationTimeAsyncImpl(UPath path, DateTime time, CancellationToken cancellationToken)
    {
        await FallbackSafe.SetCreationTimeAsync(ConvertPathToDelegate(path), time, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <inheritdoc/>
    protected override async ValueTask<System.DateTime> GetLastAccessTimeAsyncImpl(UPath path, CancellationToken cancellationToken)
    {
        return await FallbackSafe.GetLastAccessTimeAsync(ConvertPathToDelegate(path), cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <inheritdoc/>
    protected override async ValueTask SetLastAccessTimeAsyncImpl(UPath path, DateTime time, CancellationToken cancellationToken)
    {
        await FallbackSafe.SetLastAccessTimeAsync(ConvertPathToDelegate(path), time, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <inheritdoc/>
    protected override async ValueTask<System.DateTime> GetLastWriteTimeAsyncImpl(UPath path, CancellationToken cancellationToken)
    {
        return await FallbackSafe.GetLastWriteTimeAsync(ConvertPathToDelegate(path), cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <inheritdoc/>
    protected override async ValueTask SetLastWriteTimeAsyncImpl(UPath path, DateTime time, CancellationToken cancellationToken)
    {
        await FallbackSafe.SetLastWriteTimeAsync(ConvertPathToDelegate(path), time, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <inheritdoc/>
    protected override async ValueTask CreateSymbolicLinkAsyncImpl(UPath path, UPath pathToTarget, CancellationToken cancellationToken)
    {
        await FallbackSafe.CreateSymbolicLinkAsync(ConvertPathToDelegate(path), ConvertPathToDelegate(pathToTarget), cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <inheritdoc/>
    protected override async ValueTask<(bool Success, UPath ResolvedPath)> TryResolveLinkTargetAsyncImpl(UPath linkPath, CancellationToken cancellationToken)
    {
        var resolvedPath = default(UPath);
        var resolvedPathDelegateResult = await FallbackSafe.TryResolveLinkTargetAsync(ConvertPathToDelegate(linkPath), cancellationToken: cancellationToken).ConfigureAwait(false);
        if (!resolvedPathDelegateResult.Success)
        {
            resolvedPath = default;
            return (false, resolvedPath);
        }

        resolvedPath = ConvertPathFromDelegate(resolvedPathDelegateResult.ResolvedPath);
        return (true, resolvedPath);
    }

    /// <inheritdoc/>
    protected override async IAsyncEnumerable<Zio.UPath> EnumeratePathsAsyncImpl(UPath path, string searchPattern, SearchOption searchOption, SearchTarget searchTarget, [EnumeratorCancellation] CancellationToken cancellationToken)
    {
        await foreach (var subPath in FallbackSafe.EnumeratePathsAsync(ConvertPathToDelegate(path), searchPattern, searchOption, searchTarget, cancellationToken: cancellationToken).WithCancellation(cancellationToken).ConfigureAwait(false))
        {
            yield return ConvertPathFromDelegate(subPath);
        }
    }

    /// <inheritdoc/>
    protected override async IAsyncEnumerable<Zio.FileSystemItemAsync> EnumerateItemsAsyncImpl(UPath path, SearchOption searchOption, SearchPredicateAsync? searchPredicate, [EnumeratorCancellation] CancellationToken cancellationToken)
    {
        await foreach (var subItem in FallbackSafe.EnumerateItemsAsync(ConvertPathToDelegate(path), searchOption, searchPredicate, cancellationToken: cancellationToken).WithCancellation(cancellationToken).ConfigureAwait(false))
        {
            var localItem = subItem;
            localItem.Path = ConvertPathFromDelegate(localItem.Path);
            yield return localItem;
        }
    }

    /// <inheritdoc/>
    protected override async ValueTask<System.Boolean> CanWatchAsyncImpl(UPath path, CancellationToken cancellationToken)
    {
        return await FallbackSafe.CanWatchAsync(ConvertPathToDelegate(path), cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <inheritdoc/>
    protected override async ValueTask<Zio.IFileSystemWatcherAsync> WatchAsyncImpl(UPath path, CancellationToken cancellationToken)
    {
        return await FallbackSafe.WatchAsync(ConvertPathToDelegate(path), cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <inheritdoc/>
    protected override async ValueTask<System.String> ConvertPathToInternalAsyncImpl(UPath path, CancellationToken cancellationToken)
    {
        return await FallbackSafe.ConvertPathToInternalAsync(ConvertPathToDelegate(path), cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <inheritdoc/>
    protected override async ValueTask<Zio.UPath> ConvertPathFromInternalAsyncImpl(string innerPath, CancellationToken cancellationToken)
    {
        return ConvertPathFromDelegate(await FallbackSafe.ConvertPathFromInternalAsync(innerPath, cancellationToken: cancellationToken).ConfigureAwait(false));
    }

    /// <summary>
    /// Converts the specified path to the path supported by the underlying <see cref = "Fallback"/>
    /// </summary>
    /// <param name = "path">The path exposed by this filesystem</param>
    /// <returns>A new path translated to the delegate path</returns>
    protected abstract UPath ConvertPathToDelegate(UPath path);
    /// <summary>
    /// Converts the specified delegate path to the path exposed by this filesystem.
    /// </summary>
    /// <param name = "path">The path used by the underlying <see cref = "Fallback"/></param>
    /// <returns>A new path translated to this filesystem</returns>
    protected abstract UPath ConvertPathFromDelegate(UPath path);
    protected override async ValueTask<(IFileSystemAsync FileSystem, UPath Path)> ResolvePathAsyncImpl(UPath path, CancellationToken cancellationToken)
    {
        var fallback = Fallback;
        if (fallback is null)
        {
            return await base.ResolvePathAsyncImpl(path, cancellationToken: cancellationToken).ConfigureAwait(false);
        }

        return await fallback.ResolvePathAsync(ConvertPathToDelegate(path), cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}
#endif
